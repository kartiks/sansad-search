# Project Tracker

PRD version at last update: v3.3
Last updated: 2026-06-22 (/resume reconciliation — Phase 18 marked ✓ complete; both reviews CLEAR on latest runs. All 18 phases now ✓ complete. Prior: /verify-qa phase-18 — QA review run-1 CLEAR, no gaps. F02 Snippet Size Parameter + F05 effective-size snippet + NFR PERF-4 fully covered by non-vacuous tests (clamp boundaries incl. 0/neg→20, 100.5→default not rounded, never-a-400 incl. list/dict, default 100-not-200, short-text no-padding, operator default + out-of-range clamp). 169 passed in the two search test files; full backend 1208 passed/24 skipped (skips unrelated Phase 17 PG). PERF-4 live-corpus latency = manual verification (not unit-testable, max-bound clamp IS tested). Both phase-18 reviews now CLEAR → /resume reconciliation marks ✓ complete. Report: .agentic-reviews/qa/qa-review-phase-18-run-1.md. Prior: 2026-06-22 (/build phase-18 — configurable snippet size implemented. routes/search.py `SearchRequest` +optional `snippet_size` (Any, never a 400); services/search.py new `resolve_effective_snippet_size` (clamp 20–1000, else env `SNIPPET_DEFAULT_WORDS` default 100, default also clamped per PERF-4) drives dynamic Meilisearch `cropLength`, replacing hardcoded `SNIPPET_WORD_WINDOW=200`; `_build_snippet`/`format_result` take `snippet_words`. Tests rewritten/added in test_search_route.py + test_search_service.py (clamp boundaries, fallback, never-validation-error, operator-default env). Backend 1208 passed/24 skipped (skips = env-gated PG tests from Phase 17, unrelated). PERF-4 p95 latency at snippet_size=1000 flagged for manual live-corpus verification (bound clamp is unit-tested). Phase 18 Build=complete. Next: /verify-arch phase-18 then /verify-qa phase-18. Prior: 2026-06-22 (/spec — configurable snippet size. F02 gains optional `snippet_size` API param (integer words, range 20–1000, clamped to nearest bound; non-integer/non-numeric/missing → default); F05 snippet generalized from fixed ≥200-word floor to effective snippet size; default LOWERED 200→100 words, operator-configurable; new NFR PERF-4 (snippet size bound, PERF-1 must hold at snippet_size=1000). UI exposes no control — param is for programmatic API consumers. Files: 02-search.md +Snippet Size Parameter/ACs/NFR-impl, 02-search-tests.md +clamp boundaries, 05-result-display.md (card tables + Snippet Generation reframed), 05-result-display-tests.md (Snippet Minimum Size → Snippet Size), 04-non-functional-requirements.md +PERF-4. Generated PRD-v3.3 + PRD-SUMMARY-v3.3 + DIFF-v3.2-to-v3.3. Downstream: behavior change (default 200→100) touches already-built F05 + new API surface → run /resume to assess; new phase needed via /plan then /build. Prior: 2026-06-14 (Opportunistic fix: app/pyproject.toml build-backend corrected `setuptools.backends.legacy:build` → `setuptools.build_meta` — latent only, deps install via requirements.txt, but pip install . / wheel builds would have failed. Phase 17 arch-review run-2 escalation (3) resolved. Prior: /resume — README.md Data Sources table + Setup and Run updated to PRD v3.2 source chain; all 17 phases ✓ complete; no pending phases; remaining items are operator/deploy tasks outside framework scope. Prior: /resume reconciliation — Phase 17 ✓ complete; both reviews CLEAR. Prior: Phase 17 build complete — checkpoint store SQLite→PostgreSQL + Railway Cron Job task list. schema.sql adds processed_documents (PK canonical_doc_id+corpus, idx_processed_documents_corpus) + ingestion_dedup_keys (PK dedup_key+corpus, idx_ingestion_dedup_keys_corpus) per DATA-MODELS §1.6/§1.7. store.py fully rewritten to psycopg2 (DATABASE_URL DSN, _reconnect/_is_dead_connection resilience mirroring indexer.py; no sqlite3, no data/ingestion_checkpoints.db); add_dedup_key now takes optional corpus (derives from dedup_key source-prefix when omitted). **§8 item-7 fix (user-ratified):** indexer.index_record no longer short-circuits on checkpoint.has_dedup_key — the ingestion_dedup_keys mirror is a pre-filter only; UNIQUE(dedup_key) ON CONFLICT is the authoritative guard; this is what lets a selective re-process (clear speeches/qa+processed_documents, keep mirror) re-create rows. main.py: CheckpointStore(pg_dsn). clear_corpus.py: --target sqlite→checkpoints (PG DELETE; date-scoped processed_documents via DELETE…USING raw_documents join; --stage fetch clears both checkpoint tables, --stage process clears processed_documents only). .gitignore: dropped 3 ingestion_checkpoints.db* entries. Tests: new tests/fakes.py FakeCheckpointStore (in-memory, drop-in) for unit tests (test_indexer/test_phase12/orchestrator imports repointed); test_checkpoint_store.py rewritten for real PG (TEST_DATABASE_URL-gated) incl. §8 item-7 regression + INF-R1 interrupt/resume; dedup unit tests reworked to ON CONFLICT via new dedup-aware mock PG conn; new tests/test_clear_corpus.py (--target sqlite rejected, --target checkpoints/--stage process/fetch + date-scoped join + dry-run). Full backend suite 1201 passed/0 failed (run with DATABASE_URL + TEST_DATABASE_URL → local throwaway sansadsearch_test DB; PG-gated checkpoint tests executed for real, not skipped). **OPEN deployment task (manual, no automated test — per phase stop condition):** provision Railway Cron Job per DEPLOYMENT §3.7 (start cmd `python -m ingest.main --source all`; set DATABASE_URL/MEILISEARCH_URL/MEILISEARCH_MASTER_KEY on Cron Job only; MASTER_KEY must NOT be on API service). **Migration for existing deploys (no re-ingestion):** DEPLOYMENT §6.7 (create 2 tables + backfill from speeches/qa+raw_documents + rm SQLite file). Routing note: PRD F01 lines 115/120 still say "SQLite checkpoint store" — Product Agent /spec change, non-blocking. Next: /verify-arch phase-17 then /verify-qa phase-17. — Prior: 2026-06-12 (Bug fix: indexer.py _is_dead_connection now handles psycopg2.InterfaceError("connection already closed") — previously only OperationalError triggered reconnect; InterfaceError fell through to the duplicate-skip branch, silently dropping records during disk-full incidents. Fix: added InterfaceError check before OperationalError check; returns True when message contains "connection already closed". 1 new test in TestIndexerConnectionResilience. 1189 passed, 2 skipped.) — Prior: Bug fix: Q+A questioner_names semicolon split — _get_list in ia_text_parser was splitting eparlib_members on comma; actual IA metadata delimiter is semicolon. Multi-questioner values like "A; B; C" were stored as a single-element array. Fixed: string path now splits on ";", both paths canonicalize via " ".join(part.split()) to collapse internal whitespace. 2 new tests + 1 renamed test in TestEparlibQAMetadataFields. Re-ingestion required: Stage 2 re-process of Q+A scope + Meili reindex (no dedup key impact, no duplicates). 1188 passed, 2 skipped.) — Prior: Bug fix: indexer.py read-path connection resilience — the three read methods (check_raw_document_exists, read_raw_documents_for_scope, reindex_from_db) had no dead-connection retry, so a connection dropped during long IA discovery phases would raise instead of recovering like the write methods. New private _execute_read(sql, params) helper executes the read, reconnects via _reconnect() and retries once on a dead-connection OperationalError (pgcode=None / "server closed"), then returns the live cursor so each caller keeps its natural fetch pattern (fetchone for the existence check, fetchall for bulk reads); non-dead errors propagate unchanged. 4 new tests in TestIndexerConnectionResilience (one per read method recovering + non-dead-error propagation). Also fixed the long-standing stale test_search_route.py::test_crop_length_is_400 — renamed to test_crop_length_is_200 and assertion corrected from 280→200 to match cropLength 400→200 (PRD v3.1 F05 / DATA-MODELS §2.3 / SNIPPET_WORD_WINDOW=200). Full backend now fully green: 1186 passed, 2 skipped, 0 failures (DATABASE_URL set for main-routing tests).) — Prior: UI fix: suggested query chips wrap alignment — chips wrapped in .suggested-queries-chips div with flex-wrap:wrap; flex-wrap removed from .suggested-queries-row; wrapped lines now align with first chip, not "Try:" label. 521 frontend passing.) — Prior: Bug fix: indexer.py PostgreSQL connection timeout resilience — Bug 1: rollback() calls now wrapped in try/except InterfaceError in index_record, write_raw_document, update_index_status; Bug 2: Indexer accepts pg_dsn param, _reconnect() method added, dead-connection OperationalError (pgcode=None) triggers reconnect+retry-once in all three write methods; main.py passes DATABASE_URL DSN to Indexer; 3 new tests added — 45/45 passing.) — Prior: Phase 16 arch review run-3 CLEAR — Home.jsx ESM import ordering gap resolved; all 10 Non-Negotiables CLEAR; phase 16 ✓ complete.) — Prior: 2026-06-11 (Phase 16 arch gap fix: Home.jsx ESM import ordering corrected — all imports now precede const/function declarations; 521 frontend passing.) — Prior: 2026-06-11 (Phase 16 arch review run-2 GAPS FOUND: same 1 minor gap — Home.jsx ESM import ordering unresolved. All 10 Non-Negotiables CLEAR; no API/schema/data-flow changes.) — Prior: 2026-06-11 (QA review Phase 16 run-2 CLEAR — all 4 run-1 gaps resolved; 521 frontend passing.) — Prior: 2026-06-11 (Phase 16 arch review run-1 GAPS FOUND: 1 minor gap — Home.jsx ESM import ordering.) — Prior: 2026-06-11 (Phase 16 gap-fix: 8 new frontend tests address all 4 QA run-1 gaps — 521 frontend passing.) — Prior: 2026-06-11 (QA review Phase 16 run-1 GAPS FOUND — 4 gaps all to Coding Agent: F03 type-clear untested, F10 debug-badge in Results header untested, F09/F10 detail-debug-badge in RecordDetail untested, F09 RecordDetail sticky header elements untested.) — Prior: 2026-06-10 (Load control button styling fixed: centered, outlined, grey text when disabled; record-load-btn CSS added; adjacent card gaps unified to 12px; fulltext card gets 12px bottom margin; order confirmed as load-prev → prev records → metadata → focal fulltext → next records → load-next. — UI agent changes implemented: (1) Body color system — card 3px left borders + colored body badges in SpeechCard/QACard/RecordDetail, modal toggle chips for Legislative Body (body-colored) + Proceeding Type (navy), homepage corpus pills. (2) Homepage corpus pills row + suggested queries row (4 pools, random chip per pool per load, hidden on typing, auto-submits). (3) AdvancedSearchModal: Legislative Body checkboxes → body-colored toggle chips; Proceeding Type checkboxes → navy toggle chips + Select all/Clear actions; Subject placeholder/helper text updated. (4) Proceeding badge: 11px→12px + 1px border. (5) Saved search filter summary: subject added as "Subject: [value]". (6) Debug badge (DEBUG_BADGE) added to Results page header right area when ?debug=1 active. (7) RecordDetail sticky header: wordmark + compact search bar + Advanced Search + bookmark icon; debug badge if ?debug=1. (8) Canonical text constants added to constants.js. All tests green: 1178 backend (pre-existing test_crop_length_is_400 still stale/failing) / 513 frontend. — Prior: Two post-phase fixes: (1) GET /api/status now queries live counts from speeches+qa_exchanges grouped by source instead of reading stale index_status run totals; last_updated still from index_status.run_completed_at. Status route tests rewritten (17 passing). (2) clear_corpus.py --stage now accepts 'all' as alias for 'fetch' — clears all stores including raw_documents. — Prior: ElibraryDSpace7Provider ingestion bug fixes — three issues found and fixed while testing LS 2024-01-01 to 2024-06-30 ingestion: (1) Pagination bug: discover() assumed DSpace 7 API returns items in strict ASC date order and used early termination (reached_end_of_scope) when a post-scope item appeared; API sort is unreliable so the fix changes post-scope items from break→continue, stopping only on len(objects)<PAGE_SIZE. (2) Schema constraint: raw_documents.format was VARCHAR(10) CHECK IN ('html','ia_text','pdf') — 'elibrary_text' is 13 chars and was missing from the check list; changed to VARCHAR(20) + added 'elibrary_text' in schema.sql and DATA-MODELS.md §1.4; ALTER TABLE migration run on live DB. (3) Speech segmenter: Tika-extracted elibrary text uses inline attribution format 'SHRI NAME (CONSTITUENCY): text' on one line, but segmenter only recognised standalone 'NAME:\n' format; added _INLINE_ATTRIBUTION_RE + constituency stripping + trailing-) cleanup for wrapped minister name fragments + HON. SPEAKER to _PRESIDING_OFFICER_RE. All 3 fixes include tests. 1176 backend tests passing (pre-existing test_crop_length_is_400 still stale/failing — separate issue). — Prior: Snippet size fix + other post-phase improvements. All tests green: 1167 backend / 498 frontend.)

## Lifecycle Status

| Stage | Status      | Last updated | Handoff notes |
|-------|-------------|--------------|---------------|
| SPEC  | complete    | 2026-06-22   | 2026-06-22: configurable snippet size — F02 +`snippet_size` param (20–1000, clamp, default 100 operator-configurable), F05 snippet reframed (floor→target; default 200→100), +NFR PERF-4. PRD v3.3 + PRD-SUMMARY-v3.3 + DIFF-v3.2-to-v3.3 generated. All F01–F10 still paired; no unpaired files. Downstream impact pending: default-snippet behavior change hits built F05 + net-new API param → /resume then /plan (new phase) then /build. Prior: F01–F10 complete with paired test specs. PRD v3.2 + PRD-SUMMARY-v3.2 + DIFF-v3.1-to-v3.2.md generated. 2026-06-13: /spec session confirmed Phase 17 build routing note ("SQLite checkpoint store" wording) is a stale carry-forward — F01 spec already made storage-agnostic in v3.2; no changes required. v3.2 change: F01 wording — removed SQLite qualifier from checkpoint store references (storage-agnostic); no functional changes. Prior: PRD v3.1 + PRD-SUMMARY-v3.1 + DIFF-v3.0-to-v3.1.md generated. Changes from v3.0: F01 — LS scope 1947-08-15 (was 2014-01-01); LS chain now IA + elibrary.sansad.in DSpace 7 (2019+, Tika PDF), eparlib removed; RS chain IA-only (sansad.in/rs JS-rendered removed, rsdebate.nic.in removed), post-2018 RS unavailable, RS coverage note added. F03 — Subject filter added (§5, substring match on subject field); LS/RS date min updated to 1947-08-15; F08 saved search filter state updated to include subject. F04 — Query Preprocessing section added (U+201C/U+201D → ASCII quotes). F05 — snippet size ≥200 words (was ≥400 words in v3.0; card table descriptions also corrected from stale "2–3 sentences"). NFR INF-RL1 updated (removed eparlib/sansad.in/rsdebate from compliance list). PENDING: DATA-MODELS.md §2.3 cropLength needs update from 400→200 (run /arch or update manually). |
| UI    | complete    | 2026-06-10   | 02-ui-ux-spec.md reconciled to PRD v3.1. 4 mockups current (homepage, results-page-with-filters, advanced-search-modal-ca-only, record-detail-page). Changes from v1.0: (1) Body color system added (CA ochre #8B6914, LS green #1E6B35, RS crimson #9B1D20) — card 3px left borders + colored body badges + modal toggle chips + homepage corpus pills. (2) Homepage: corpus pills row + suggested queries (4 categories, random per load). (3) Advanced Search Modal: checkbox groups replaced with toggle chips; Subject field added (field 5 of 6). (4) Proceeding type badge: 11→12px + 1px border. (5) F09 Record Detail Page: full new screen spec (metadata panel, focal record, adjacent load controls, back nav, 404 state). (6) F10 Debug Mode: global search debug panel + per-card debug toggle + DEBUG_BADGE in header. (7) Navigation model updated to 3 views. (8) Saved search filter summary includes subject. (9) Canonical text expanded (21 strings). 01-personas.md unchanged — 4 personas still accurate. No open decisions. Next: /arch (UI spec may have infrastructure implications for suggested query randomization — client-side only, no new infra required). |
| ARCH  | complete    | 2026-06-22   | **Re-run 2026-06-22 (doc reconciliation — closes Phase 18 arch review escalations 1 & 2):** Bumped top-line PRD version headers — ARCHITECTURE v3.1→v3.3, DATA-MODELS v3.1→v3.3, DEPLOYMENT v3.2→v3.3 (bodies already carried the v3.3 changelog; this was the cosmetic header lag, escalation 2). Fixed DATA-MODELS §2.3 snippet-source wording (escalation 1): the returned `snippet` is built in Python by `_build_snippet` (sliding word-window over `full_text_en` sized to the effective snippet size) — that is the operative lever; the dynamic Meilisearch `cropLength` is still sent but is **non-operative** for the returned `snippet` (prior text wrongly named `_formatted.full_text_en` / `cropLength` as the source). Behavior unchanged — Phase 18 already wired `_build_snippet`'s window to `effective_snippet_size`, so this is wording-only; no code, schema, index-settings, services, or non-negotiable changes. Changelog notes appended to all 3 docs. PENDING: user review + commit all 3 arch docs. --- **Re-run 2026-06-22: PRD v3.3 — configurable snippet size.** All 3 arch docs reconciled v3.2→v3.3 (headers bumped). User-ratified decisions: (1) `snippet_size` is a top-level optional integer field on the `POST /api/search` JSON body (sibling of query/sort/page) — NOT a query param, NOT nested in filters; (2) operator default via new optional API env var `SNIPPET_DEFAULT_WORDS` (default 100). Changes: DATA-MODELS §2.3 — `cropLength` now **dynamic** = effective snippet size (was hardcoded 200); new "Effective snippet size" resolution rule (JSON integer→clamp 20–1000 inclusive; absent/empty/non-numeric/non-integer e.g. 100.5→default; 0/neg/19→20, 1001/5000→1000; never a validation error); debug-envelope example cropLength 200→100. DATA-MODELS §3.1 — `snippet_size` added to request body + field rules. ARCHITECTURE §3 search.py comment + §4 search-request data-flow row → dynamic cropLength/effective size. DEPLOYMENT §2.1 — `SNIPPET_DEFAULT_WORDS` (Optional, default 100); §6 snippet note → dynamic, no re-index/migration; env-var change needs only API restart. NFR PERF-4 captured (bound 20–1000 so PERF-1 holds at 1000; per-request resolution clamps even if default set out of range). No new files/services/non-negotiables/deps/schema/index-settings. Downstream: behavior change (default 200→100) hits already-built F05 + net-new `snippet_size` API surface → forward-only via NEW phase: run /plan then /build. Build must update existing tests asserting 200→100 and verify PERF-4 at snippet_size=1000. PENDING: user review + commit all 3 arch docs. --- **Re-run 2026-06-14: DEPLOYMENT.md + README.md verification pass.** Header bumped DEPLOYMENT v3.1→v3.2 (v3.1→v3.2 diff is wording-only). Fixed 3 DEPLOYMENT.md defects: (1) §6.3 "PRD v2.0 migration" body had been displaced below §6.5 (header was empty) — relocated under its header; §6.1–6.7 now sequential. (2) §6.6 F05 snippet figure clarified (cropLength ≥400 at v3.0 / ≥200 at v3.1; was stale "≥400"). (3) review note appended to header changelog. Verified §6.7 DDL matches DATA-MODELS §1.6/§1.7 exactly; §3.3 table list + key-isolation (search-key on API / master-key on Cron) consistent with DATA-MODELS/ARCHITECTURE. **DEFERRED to /verify-arch (needs /app/ read, out of /arch boundary):** (a) §3.1 says "Railway detects from pyproject.toml" but build cmd + §5.2 use `pip install -r requirements.txt` — confirm which manifest the repo ships / Railway uses; (b) §3.2 vercel.json proxy URL `sansad-search-dev-api-production.up.railway.app` matches live Railway service URL. **ROUTED TO PROJECT MANAGER (README.md is an INIT artifact, not editable by /arch):** README.md is stale on two counts — (1) Data Sources table (lines 28-32) predates the v1.2 ingestion redesign: CA shown as "sansad.in" should be constitutionofindia.net (HTML); LS shown as "sansad.in" should be Internet Archive (archive.org) + eparlib.sansad.in DSpace; RS shown as "rajyasabha.gov.in" should be sansad.in/rs HTML + Internet Archive + rsdebate.nic.in (rajyasabha.gov.in unused). (2) "Setup and Run" (lines 34-36) still says "instructions will be added after the architecture phase / run /arch" — arch done + 17 phases built; should point to DEPLOYMENT.md §5. PENDING: user commit of DEPLOYMENT.md; PM session to fix README; /verify-arch for the 2 /app/ items. --- **Re-run 2026-06-12: ingestion checkpoint store moved local SQLite → PostgreSQL; pipeline made deployable as a Railway Cron Job.** All 3 arch docs updated (headers note 2026-06-12). ARCHITECTURE: §1 (no offline/local qualifier), §2 (new "Ingestion hosting | Railway Cron Job" row), §3 (store.py comment → PG; schema.sql lists 2 new tables; removed `data/ingestion_checkpoints.db` note), §4 (Stage 2 flow → PG checkpoint), §5 (rewrote "Checkpoint store — dual signal" → both signals in PG), §7 Non-Neg #7 reworded ("standalone CLI process — never via the production API"; Cron Job is a CLI process, intent preserved) + #10 (SQLite→PG), §8 new build-invariant item 7 (dedup-mirror must not suppress legitimate inserts on re-process; authoritative guard = UNIQUE(dedup_key) ON CONFLICT). DATA-MODELS: new §1.6 `processed_documents` (PK canonical_doc_id+corpus) + §1.7 `ingestion_dedup_keys` (PK dedup_key+corpus) with fields/PK/indexes/query patterns; §1.4/§1.5 SQLite refs → PG; §4.1 table list +2; §4.3 "Local SQLite" replaced with "Ingestion checkpoint tables (PostgreSQL)". DEPLOYMENT: §1 hosting table +Cron row; §2.3 retitled (local CLI **or** Cron) + MASTER_KEY-on-Cron-not-API warning; §3.3 schema list; §3.5 (no local file); new §3.7 (Railway Cron Job deploy — why Cron not Worker, env vars, INF-P1 long-run split, MASTER_KEY isolation); §6.1 (removed `rm …db`, TRUNCATE now 6 tables, renumbered steps); §6.4 Step 3 (sqlite3 snippet → `DELETE … USING raw_documents` join); §6.5 (`--target sqlite`→`checkpoints`, tables/flags/env updated); new §6.7 (SQLite→PG **backfill migration — NO re-ingestion**, two INSERT…SELECT, zero-record-doc caveat); new §7 "Implementation tasks for /build" (schema.sql, store.py rewrite, clear_corpus.py rename, .gitignore, tests incl. §8-item-7 regression + INF-R1 resumability, migration, Cron deploy). **OPEN — routed to /spec:** PRD F01 lines 115/120 still name "the SQLite checkpoint store"/"the SQLite `processed_documents` store" — make storage-agnostic; NOT a functional conflict (INF-R1 is medium-agnostic), does not block /build. **Decisions user-ratified:** Cron Job (not Worker); `--target sqlite`→`checkpoints`; two PG tables; migrate via backfill (no clean reindex). **Design note flagged:** `ingestion_dedup_keys` is now partly redundant (mirror in same PG instance as the UNIQUE constraint) — kept per user spec as a cheap existence-check surface; could be dropped in future to rely solely on ON CONFLICT. PENDING: user review + commit all 3 arch docs; then /build to implement §7 task list; then /spec for the F01 wording. --- **Re-run 2026-06-10: post-UI-stage assessment — no architectural changes required.** UI spec (PRD v3.1) fully reflected in existing arch docs; suggested query randomization confirmed client-side only (no new infra); all 3 arch docs remain at v3.1. No files modified this session. --- **Re-run 2026-06-09: reconciled all 3 arch docs to PRD v3.1 (headers → v3.1).** Changes: DATA-MODELS §2.3 `cropLength` 400→200 (F05); DATA-MODELS §2.3 `filterableAttributes` + `subject` added (F03); DATA-MODELS §2.4 subject filter expression added; ARCHITECTURE §3/§4 cropLength 400→200 references updated. DEPLOYMENT no substantive changes. **Re-run 2026-06-06: reconciled all 3 arch docs to PRD v3.0 (headers → v3.0).** Addressed all 4 brief gaps + 1 discovered gap. (1) **Non-Negotiable #9 reversed** — `source_url` is now the IA/archive.org item URL for LS (any path) and RS-via-IA/rsdebate; sansad.in URL for RS-via-sansad HTML; coi URL for CA; null when no IA URL derivable. Updated ARCH §7 + §3 internet_archive.py comment + §5 IA pre-OCR pattern; DATA-MODELS §1.1/§1.2 source_url + §1.4 citation_url. (2) **New columns** — DATA-MODELS §1.1: `lok_sabha_number INTEGER NULL`, `segments JSONB NULL`, `canonical_doc_id TEXT NULL`; §1.2: `lok_sabha_number`, `canonical_doc_id`. §2.2 all three EXCLUDED from Meili doc; §2.3 `lok_sabha_number` NOT filterable. DEPLOYMENT §6.6 ALTER TABLE migration (no indexes needed) + full re-ingestion. (3) **F09 range API** — DATA-MODELS §3.3 now returns `has_prev`/`has_next`+`sitting_total`+`lok_sabha_number` (dropped `adjacent.prev_id/next_id`); new §3.4 `GET /api/record/{id}/adjacent?direction&from_seq&limit` (ascending records + `has_more`, PERF-2 exempt). (4) **F10 debug** — DATA-MODELS new §3.5 `GET /api/debug/processed/{id}` + §3.6 `GET /api/debug/raw/{id}` (resolves via canonical_doc_id+source; 404 contracts); §3.1 `?debug=1` + debug envelope (processed_query + Meili request/response; per-hit _rankingScore/_rankingScoreDetails); §2.3 debug retrieval config (showRankingScore/showRankingScoreDetails/attributesToRetrieve=["*"]). ARCH §3 new api/routes/debug.py + api/services/debug.py + ResultDebugPanel/SearchDebugPanel/useAdjacent/useDebugDetail/ordinal.js; §4 four new data-flow rows; §5 new Adjacent-Merging + Debug-mode patterns; §6 Postgres-read row; §8 three v3.0 build-time verifications (IA-URL on fallback paths, lok_sabha_number surface, merge break-signal detection in flat-text formats). (5) **DISCOVERED GAP (user-approved): `canonical_doc_id` link column** — speeches/qa_exchanges had no link to raw_documents, so GET /api/debug/raw/{id} was unbuildable; added column (written by Stage 2 indexer). (6) F05 — DATA-MODELS §2.3 `cropLength:400` snippet config. **DECISIONS user-ratified:** Non-Neg #9 reversal w/ null-on-no-IA-URL; record-anchored `/api/record/{id}/adjacent`; canonical_doc_id column. PENDING: user review + commit all 3 arch docs; then PLAN can write PHASES.md (was blocked on this). --- **Prior re-run 2026-06-06: DEPLOYMENT.md post-deploy production fixes.** §2.2 VITE_API_URL removed (not used in production — proxied via vercel.json); §3.1 app/.python-version=3.12 Railway/Nixpacks pin added; §3.2 app/ui/vercel.json (proxy+SPA rewrites, exact content inlined) + app/ui/.npmrc (legacy-peer-deps=true) added as required committed files; §4 Vercel row updated. Header bumped to v2.1. No architectural decisions changed. **Re-run 2026-06-04: PRD v2.1 reconciliation + Phase 12 arch run-4 doc gaps closed.** ARCHITECTURE §1/§3/§4/§5/§6 updated: two-stage pipeline (--stage fetch|process|all), new Stage 1+2 data flows, two new §5 patterns (deferred processing, dual-signal checkpoint), indexer.py + schema.sql + main.py comments updated. DATA-MODELS §1.4 raw_documents added (TEXT PK canonical_doc_id, corpus, date, provider, format, extracted_text, metadata_json, fetch_url, citation_url, fetched_at; idx_raw_documents_corpus_date); §1.4 Dedup Keys renumbered to §1.5; §4.1 tables updated; §4.3 SQLite processed_documents semantics updated to Stage 2 complete signal. DEPLOYMENT §3.3/§3.5 updated; §6.1 TRUNCATE includes raw_documents; §6.4 selective re-processing runbook added. No new env vars, dependencies, or infrastructure. Pending: user review + commit; then /plan for new phase(s) covering raw_documents table + two-stage pipeline implementation. --- Prior: **Re-run 2026-06-01: arch docs reconciled to PRD v2.0.** DECISION (user-ratified): F09 record-detail page served from **PostgreSQL** (new record-serving read path: `GET /api/record/{id}` = `speeches` UNION `qa_exchanges` by id, 404 if neither; adjacent nav = same-sitting neighbour query ordered by `sequence_within_sitting`). Search stays Meilisearch-only — deliberate split (search→Meili, detail→Postgres). Changes written across all 3 docs: ARCHITECTURE — §1 detail view; §3 new files (api/routes/record.py, api/services/record.py, ui pages/RecordDetail.jsx, hooks/useRecord.js, main.jsx /record/:id route, SpeechCard/QACard F05 badge+time); §4 new Record-detail flow; §5 three new patterns (Postgres detail, unified sitting-level sequence, CA field-level parsing); §6 Postgres Read row updated; new §8 Build-Time Verifications (shared speech↔Q+A sequence across 3 providers, CA TOC anchor mapping, time_of_day HTML-only surface). DATA-MODELS — §1.1/§1.2 added `lang_original`(NOT NULL CHECK en/hi/mixed), `time_of_day`(VARCHAR(5) HH:MM verbatim, HTML-only), `word_count`(INT null); `sequence_within_sitting` added to qa_exchanges (shared space, NOT in qa dedup key); idx_speeches_sitting + idx_qa_sitting composites; id stability rule (stable on incremental re-run via ON CONFLICT DO NOTHING, reassigned on full clean reindex). §2.2 Meili doc gains lang_original+time_of_day (both) + sequence_within_sitting (qa); word_count added to Postgres-only excluded list. §3.1 results gain lang_original+time_of_day; new §3.3 GET /api/record/{id} contract (full fields + sitting_total + adjacent{prev_id,next_id} + 404). DEPLOYMENT — new §6.3 v2.0 migration (ALTER TABLE add columns/indexes + mandatory full clean reindex since new fields need re-parse; Meili-only reindex insufficient); no new env vars/deps/infra; PERF-2 met by PK lookup + idx_*_sitting. Headers: all 3 → v2.0. PENDING: user review of changes + commit; then /plan (new phase for F01 v2.0 fields + F05 + F09 — re-ingestion required). --- Prior re-run 2026-05-31: arch docs reconciled to PRD v1.3 (driver: Phase 9 arch review run-1 GAPS FOUND — docs-only, code already correct). All 6 review gaps closed (wording-only): ARCHITECTURE Non-Neg #9 split LS (eparlib_document_url) vs RS-via-IA (rsdebate.nic.in derived from handle N; null on no-derivable-handle per PRD v1.3); §5 IA pre-OCR path same LS/RS split + dual-corpus InternetArchiveProvider ratified; §3 internet_archive.py folder comment updated. DATA-MODELS §1.1 + §1.2 source_url descriptions distinguish LS vs RS IA path + null edge case. Headers: ARCHITECTURE + DATA-MODELS → v1.3; DEPLOYMENT → v1.3 (no deployment-relevant change). Also resolved a stray git merge-conflict marker in ARCHITECTURE §3 main.jsx routes (kept /index-status route). No tech/structural decisions. Pending: user review + commit; then Phase 9 ready for /verify-arch re-run (run-2 expected CLEAR) and /verify-qa. --- Prior (v1.2): Ingestion source integration redesign, now reconciled to PRD v1.2 (all 3 arch doc headers → v1.2). Root cause: hardcoded CA volume PDF URLs on sansad.in dead (500s). Design = multi-provider chain per corpus with runtime listing-page discovery. CA = constitutionofindia.net HTML only (167 sittings, 12 vols), no OCR; eparlib handle /4 (CA Legislative = interim legislature) EXCLUDED. LS = [Internet Archive pre-OCR _djvu.txt, eparlib.sansad.in DSpace fallback]. RS = [sansad.in/rs HTML, Internet Archive, rsdebate.nic.in DSpace]. Cross-source identity = DSpace handle N (IA eparlib.nic.in.{N} ↔ 123456789/{N}); checkpoint store generalized processed_urls→processed_documents(canonical_doc_id). DSpace bitstream URLs always read from item page, never constructed. Non-Negotiable #9 = IA records cite eparlib_document_url, never archive.org. **v1.2 reconciliation: OCR removed pipeline-wide** — Tesseract dropped from ARCHITECTURE/DEPLOYMENT; pdf_parser is embedded-text-only (text-less PDFs skipped); **ocr_low_confidence DROPPED from speeches** (DATA-MODELS §1.1 + §2.2 excluded list) → this is a SCHEMA CHANGE for /plan (column built in Phases 1–3; drop is forward-only in the new phase, incl. schema.sql + any code/tests referencing it). Files updated: ARCHITECTURE.md, DATA-MODELS.md, DEPLOYMENT.md. ROUTED BACK TO /spec (2 minor): (a) source_url canonical-citation rule names only LS — extend to RS-via-IA, or drop IA from RS chain if RS not on IA; (b) F01 LS format "HTML and PDF" inaccurate (LS chain = IA text + DSpace PDF, no HTML). ESCALATION OPEN: /plan — all 6 phases ✓ complete; needs NEW phase(s) (suggest split CA-HTML / LS-IA+eparlib / RS-html+IA+rsdebate; include ocr_low_confidence drop). Build-time verification: confirm RS items exist on IA + rsdebate↔IA join key. |
| PLAN  | complete    | 2026-06-22   | PRD v3.3 reviewed 2026-06-22 (DIFF-v3.2-to-v3.3). Phase 18 added — configurable snippet size (F02 optional `snippet_size` param; F05 snippet sized to effective snippet size, default 200→100; NFR PERF-4). Backend-only: routes/search.py accepts the optional field, services/search.py resolves effective size (clamp 20–1000, else SNIPPET_DEFAULT_WORDS default 100) → dynamic Meilisearch cropLength replacing hardcoded 200; existing cropLength-200 tests updated to 100/dynamic. No UI control (programmatic consumers only); forward-only fix since v3.3 touches already-complete F05 (Phase 14/v3.1) — completed phases not reopened per Phase Rework Policy. ARCH refs: DATA-MODELS §2.3/§3.1, DEPLOYMENT §2.1, ARCHITECTURE §3/§4. Non-blocking note: arch-doc top-line `PRD version:` headers still read v3.1 (ARCHITECTURE/DATA-MODELS) / v3.2 (DEPLOYMENT) though all three carry the 2026-06-22 v3.3 changelog + content — cosmetic lag for /arch to bump, does not affect /build. Next: /build phase-18. Prior: PRD v3.2 reviewed 2026-06-13. Phase 16 added to PHASES.md (UI v3.1 — body colour system, homepage corpus pills/suggested queries, Advanced Search toggle chips, debug badge, RecordDetail sticky header; ✓ complete per TRACKER). Phase 17 added — checkpoint store SQLite→PostgreSQL + Railway Cron Job (DEPLOYMENT §3.7/§6.5/§6.7; ARCHITECTURE §8 item 7); implement store.py rewrite + schema.sql additions + clear_corpus.py rename + Railway Cron Job service provision. |

## Phase Status

| Phase | PRD version | Build | Arch Review | QA Review | Complete | Handoff notes |
|-------|-------------|-------|-------------|-----------|----------|---------------|
| 1 | v1.0 | complete | run-2 CLEAR | run-3 CLEAR | ✓ complete | Both reviews CLEAR on latest runs. Q+A Case 3 bilingual test now uses two separate asserts (English original + translated Hindi portions). 203 passed, 1 skipped. |
| 2 | v1.0 | complete | run-4 CLEAR | run-2 CLEAR | ✓ complete | Both reviews CLEAR on latest runs. All 13 run-1 gaps verified RESOLVED with strong assertions. 360 passed, 1 skipped. |
| 3 | v1.0 | complete | run-2 CLEAR | run-2 CLEAR | ✓ complete | Both reviews CLEAR on latest runs. QA run-2 verified all 12 run-1 gaps RESOLVED via mock-testable assertions + Meilisearch config invariants (rankingRules ordering, filterableAttributes membership, dictionary no-ambiguity invariant, no date in rankingRules); fixture-backed integration testing documented as deferred. 520 passed, 1 skipped. Marked ✓ complete by /resume reconciliation 2026-05-28. |
| 4 | v1.1 | complete | run-2 CLEAR | run-3 CLEAR | ✓ complete | Marked ✓ complete by /resume reconciliation 2026-05-29. QA run-3 CLEAR: footer-link gap confirmed resolved (Results.test.jsx:445 asserts href='/index-status'). 163/163 passing. Both reviews CLEAR. |
| 5 | v1.1 | complete | run-1 CLEAR | run-2 CLEAR | ✓ complete | Frontend Advanced Search Modal + filter chips (F03 UI). Arch run-1 CLEAR. QA run-2 CLEAR (2026-05-29): both run-1 gaps RESOLVED with strong assertions — (1) Home.test.jsx:354 asserts nav state.filters.sources===['LS','RS'] after unchecking CA + submit; (2) Results.test.jsx:690 mocks zero results + body filter, asserts empty-state AND filter-chips-clear-all both render. 210/210 passing, no new gaps. Backend F03 not re-audited (Phase 3 QA run-2 CLEAR). Responsive = browser-verify only. Marked ✓ complete by /resume reconciliation 2026-05-29. |
| 6 | v1.1 | complete | run-4 CLEAR | run-2 CLEAR | ✓ complete | Both reviews CLEAR on latest runs. QA run-2 (2026-05-29) re-audited all 7 run-1 gaps — all RESOLVED with strong, behavior-verifying tests: recent re-run default filters (Results.test.jsx:761), saved restore exact (Results.test.jsx:820), default sort on re-run (Results.test.jsx:816/864), auto-record (Home.test.jsx:406 / Results.test.jsx:868), save-current captures query+filters (Results.test.jsx:892), recent {expires:30} + saved persistent (Home.test.jsx:432 / Results.test.jsx:931), 500-char storage (useCookieHistory.test.js:99 / useSavedSearches.test.js:156). 307/307 passing, no new gaps, no vacuous passes. Marked ✓ complete by /resume reconciliation 2026-05-29. |
| 7 | v1.3 | complete | run-2 CLEAR | run-1 CLEAR | ✓ complete | Both reviews CLEAR. QA run-1 CLEAR 2026-05-30: all 10 Phase 7 deliverables covered (schema ocr_low_confidence drop, _http/_discovery/_provider, ia_text_parser, pdf_parser no-OCR, checkpoint store redesign, indexer, setup_meilisearch §2.3 verification). 581 passed, 1 skipped (env-gated). Marked ✓ complete by /resume reconciliation 2026-05-30. Note: PHASES.md header has an unresolved merge-conflict marker (lines 3-9; Phase 7 body intact) — resolve before next /plan. Next: /build phase-8. |
| 8 | v1.3 | complete | run-2 CLEAR | run-2 CLEAR | ✓ complete | Both reviews CLEAR on latest runs. QA run-2 (2026-05-31) re-audited all 3 run-1 gaps — all RESOLVED with strong assertions, cross-checked vs ls.py: Gap 1 source_url==eparlib_document_url exact-match positive (test_ls_orchestrator.py:406); Gap 2 starred-question title→segment_qa→record_type=qa (test_ls_orchestrator.py:433); Gap 3 genuine IA-fetch-failure→DSpace fallback, doc not checkpointed on error (test_ls_orchestrator.py:489 vs ls.py:253-259). 728 passed/1 skipped, no new gaps, no vacuous passes. Marked ✓ complete by /resume reconciliation 2026-05-31. Next: /build phase-9. |
| 9 | v1.3 | complete | run-2 CLEAR | run-2 CLEAR | ✓ complete | Both reviews CLEAR on latest runs. QA run-2 (2026-05-31) re-audited all 4 run-1 gaps — all RESOLVED with behavior-bearing assertions, cross-checked vs rs.py/sansad_rs_html.py: Gap 1 default chain order+membership exact `[SansadRsHtml, IA(RS), RsdebateDspace]` (test_rs_orchestrator.py:145 vs rs.py:225-229); Gap 2 fetch-failure not checkpointed (test:366 vs rs.py:257-263); Gap 3 parse-exception + unrecognised-format both errors==1/not checkpointed (test:370/:386 vs rs.py:325-338); Gap 4 exact 2014-01-01 incl / 2013-12-31 excl (test_sansad_rs_html.py:210-241 vs sansad_rs_html.py:122). 788 passed/1 skipped, +6 over run-1, no new gaps, no vacuous passes. Marked ✓ complete by /resume reconciliation 2026-05-31. |
| 10 | v2.0 | complete | run-3 CLEAR | run-2 CLEAR | ✓ complete | Arch run-3 CLEAR 2026-06-03: both run-2 items RESOLVED — (1) unreachable date guard removed (ca.py); (2) ARCHITECTURE.md §5 updated with all 3 CA URL slug formats. All 9 non-negotiables verified. Schema, segmenters, indexer, setup_meilisearch, CA orchestrator, html_parser all aligned to DATA-MODELS v2.0. |
| 11 | v2.0 | complete | run-1 CLEAR | run-2 CLEAR | ✓ complete | QA run-2 CLEAR 2026-06-02: both run-1 gaps RESOLVED. Gap 1 (MISSING adjacent-nav behavior): 4 behavior tests added — clicking Next/Prev updates URL to neighbour id; clicking disabled control produces no navigation (RecordDetail.test.jsx:351-412). Gap 2 (WEAK page_reference): assertion tightened to .textContent.toBe('PDF page 42'); component fixed (single span, no duplicate label). 853 backend + 371 frontend passing. Marked ✓ complete by /resume reconciliation 2026-06-02. |
| 12 | v2.1 | complete | run-6 CLEAR | run-6 CLEAR | ✓ complete | QA run-6 CLEAR 2026-06-05: 1014 passed/1 skipped. 3 new tests address arch run-5 gaps — cross-corpus SQLite PK isolation (test_cross_corpus_isolation), corpus param on is_document_processed() threaded through all Stage 2 orchestrator tests, _extract_stage1_fields() refactor regression-clean. All Phase 12 stop conditions verified: raw_documents schema, write/check/read indexer methods, Stage 1/2 CA orchestrator, resumability, main.py routing + date scoping, completion summary. Both reviews CLEAR. |
| 13 | v3.0 | complete | run-2 CLEAR | run-2 CLEAR | ✓ complete | Both reviews CLEAR on latest runs. QA run-2 (2026-06-07) re-audited both run-1 gaps — RESOLVED with strong, non-vacuous tests: Gap 1 (MISSING canonical_doc_id stamp) → test_canonical_doc_id_stamped_on_indexed_records (test_ls_orchestrator.py:553) drives full LS Stage-2 and asserts each indexed record's canonical_doc_id==fixture handle (fails if ls.py:371 seam removed); Gap 2 (EDGE CASE formal merge-break) → test_formal_house_procedural_marker_between_same_speaker_breaks_merge (test_speech_segmenter.py:538) exercises the "The House adjourned" branch of _PROCEDURAL_BREAK_RE (speech.py:94). 1065 passed/2 skipped (+2 over run-1), no failures, no new gaps. Marked ✓ complete by /resume reconciliation 2026-06-07. |
| 14 | v3.0 | complete | run-2 CLEAR | run-2 CLEAR | ✓ complete | QA run-2 CLEAR (2026-06-07): all 3 run-1 gaps resolved — Gap 1 (WEAK adjacent record fields): 2 RecordDetail tests assert speech (speaker/date/subject/type/text) + Q+A (questioner·minister attribution) by value within container; Gap 2 (FAILURE PATH error UI): 3 RecordDetail tests assert adjacent-error-next/prev message text + first batch persists after second batch fails; Gap 3 (EDGE CASE limit clamp): 2 backend tests assert limit=50 → ≤5 records and limit=0 → 1 record. 1084 backend/424 frontend passing, no failures, no new gaps. Both reviews CLEAR. |
| 15 | v3.0 | complete | run-2 CLEAR | run-2 CLEAR | ✓ complete | Both reviews CLEAR on latest runs. QA run-2 (2026-06-07) re-audited both run-1 gaps — RESOLVED with non-vacuous tests cross-checked vs implementation: Gap 1 (EDGE CASE _rankingScoreDetails-absent) → ResultDebugPanel.test.jsx:141 asserts debug-scoring-content renders with score 0.87 and no _rankingScoreDetails key (guard at ResultDebugPanel.jsx:83); Gap 2 (WEAK API-request URL) → Results.jsx:496 now sets apiRequest.url=`${SEARCH_ENDPOINT}?debug=1`, SearchDebugPanel.test.jsx:114 + fixture:20 assert URL contains ?debug=1. 1110 backend/2 skipped, 492 frontend passing, no failures, no new gaps. Marked ✓ complete by /resume reconciliation 2026-06-07. |
| 16 | v3.1 | complete | run-3 CLEAR | run-2 CLEAR | ✓ complete | Arch run-3 CLEAR (2026-06-11): run-2 minor gap resolved — Home.jsx ESM import ordering corrected, all 8 imports now precede const/function declarations. All 10 Non-Negotiables CLEAR. Both arch and QA reviews CLEAR. — Gap fix applied 2026-06-11: Home.jsx ESM import ordering corrected — 8 imports moved from lines 35-42 (after const/function declarations) to immediately follow the constants.js import block at the top of the file. 521 frontend passing. — Arch run-2 GAPS FOUND (2026-06-11): same 1 minor gap from run-1 — Home.jsx ESM import ordering (lines 35-42 placed after const/function declarations at lines 13-34) is unresolved. — QA run-2 CLEAR (2026-06-11): all 4 run-1 gaps resolved — (1) F03 type-clear; (2) F10 debug-badge in Results; (3) F10/F09 detail-debug-badge in RecordDetail; (4) F09 sticky header. 521 frontend passing. |
| 18 | v3.3 | complete | run-1 CLEAR | run-1 CLEAR | ✓ complete | QA review run-1 CLEAR 2026-06-22: no gaps. Scope = F02 Snippet Size Parameter + F05 snippet sized to effective size + NFR PERF-4 (Phase 18 backend-only); prior-phase features not re-audited (full suite is the regression net). All F02/F05/test-spec requirements covered by non-vacuous tests: clamp boundaries (0/neg→20 NOT default; 19→20; 1001→1000; 20/1000 inclusive — `test_snippet_size_*` route + `test_clamp_low/high` service), 100.5→default not rounded (`test_non_integer_float_falls_back` 100.5 AND 300.0→100), present-but-empty→default, never-a-400 (`test_malformed_snippet_size_never_a_validation_error` covers float/str/bool/null/list/dict→200), default 100-not-200 at route cropLength + `_build_snippet` word count (>=100 & <200), short-text full/no-padding (word count==9 not 300), explicit 300→cropLength 300 + window ≥300, operator default env applied + per-request override + out-of-range clamp (5000→1000, 5→20). 169 passed in the two search test files; full backend 1208 passed/24 skipped (skips = env-gated Phase 17 PG tests, unrelated); no failures, no vacuous passes. **Non-gap (not routed):** PERF-4 runtime clause (PERF-1 ≤2s p95 at snippet_size=1000 across live corpus) is a load/SLA test, not unit-testable — same class as PERF-1/PERF-2, never a QA gap in prior phases; the max-bound (1000) clamp it exists to enforce IS unit-tested; manual verification, no Coding Agent gap. **Non-gap (doc, for /arch not /build):** DATA-MODELS §2.3 cropLength-vs-Python-snippet wording mismatch (arch escalation 1) — both levers wired to effective_snippet_size and tested, behavior correct. Report: .agentic-reviews/qa/qa-review-phase-18-run-1.md. Both reviews now CLEAR — ready for /resume reconciliation to mark ✓ complete. --- Arch review run-1 CLEAR 2026-06-22: no Phase-18 architectural gaps. All 10 Non-Negotiables CLEAR (untouched — query-time cropLength + one optional request field). `snippet_size` added as `Optional[Any]` on SearchRequest (no Pydantic coercion → never a 400, matches DATA-MODELS §3.1); `resolve_effective_snippet_size` implements §2.3 resolution exactly (int→clamp[20,1000] incl.; bool/float/str/None/missing→operator default; default itself re-clamped so PERF-4 bound holds); `SNIPPET_DEFAULT_WORDS` per-request env read matches DEPLOYMENT §2.1 (default 100, invalid→100). No schema/index-settings/folder/dep changes; ARCHITECTURE §3 comment + §4 search-flow row updated. PERF-4 p95@1000 is a runtime load test (bound clamp is statically enforced + unit-tested) — correctly flagged manual, not an arch gap. **ESCALATION (1) — pre-existing, non-blocking:** DATA-MODELS §2.3 says snippet sourced from Meilisearch `_formatted.full_text_en`, but `services/search.py` generates the snippet in Python (`_build_snippet` sliding window over raw `full_text_en`); the dynamic `cropLength` sent to Meili is therefore non-operative for the returned `snippet` (operative lever = Python `snippet_words`, which Phase 18 wired to the same `effective_snippet_size`, so behavior is correct). Pre-dates Phase 18 (Phase 3 `_build_snippet` + Phase 14 cropLength, both prior-CLEAR). Recommend /arch reconcile §2.3 wording (option a) rather than a code change. **ESCALATION (2) — cosmetic, already tracked:** ARCHITECTURE/DATA-MODELS top-line header still `v3.1`, DEPLOYMENT `v3.2`, though all carry the v3.3 changelog — /arch to bump. Report: .agentic-reviews/arch/arch-review-phase-18-run-1.md. Next: /verify-qa phase-18. --- Build complete 2026-06-22 (first build run, PRD v3.3). F02/F05 configurable snippet size. `routes/search.py`: `SearchRequest` gains optional top-level `snippet_size: Optional[Any]` (Any → no Pydantic coercion; integer-ness judged from JSON type at resolution; **never a 400**), passed through to `execute_search`. `services/search.py`: new `resolve_effective_snippet_size()` per DATA-MODELS §2.3 (JSON int → clamp [20,1000] incl.; absent/empty/non-numeric/JSON-float e.g. 100.5/bool → operator default; default itself clamped so PERF-4 holds even if mis-set), `_operator_default_snippet_size()` reads env `SNIPPET_DEFAULT_WORDS` per-request (default 100, invalid→100). Hardcoded `SNIPPET_WORD_WINDOW=200` removed; `crop_length` now dynamic = effective size; `_build_snippet`/`format_result` take `snippet_words`. Tests: rewrote `TestSnippetCropParams` (crop_length default 100; 300→300; 20/1000 inclusive; 19→20,1001→1000,5→20,5000→1000,0→20,neg→20; float/""/non-numeric/null/bool→default; malformed-never-400 incl. list/dict; env default applied + clamped); new `TestResolveEffectiveSnippetSize`; `_build_snippet` default-100 + explicit-300 + short-text-no-padding. **PERF-4 latency (PERF-1 ≤2s p95 at snippet_size=1000) NOT auto-verifiable — needs live-corpus load test (the 1000 clamp bound IS unit-tested); flagged for manual verification.** No UI/schema/index/dep changes. Backend suite 1208 passed / 24 skipped (skips = env-gated PG checkpoint tests from Phase 17, TEST_DATABASE_URL unset, unrelated to this phase — pass against live DB). Results: .agentic-reviews/qa/test-results-phase-18.txt. Next: /verify-arch phase-18 then /verify-qa phase-18. |
| 17 | v3.2 | complete | run-2 CLEAR | run-1 CLEAR | ✓ complete | Arch review run-2 CLEAR 2026-06-14 (ARCH-stage-deferred /app/-read doc verification — NOT a code re-review; run-1 code verdict stands). DEPLOYMENT §3.1 manifest VERIFIED CORRECT (repo ships both app/pyproject.toml [dependencies=[], requires-python>=3.12, detection-only] + app/requirements.txt [12 real deps installed via documented `pip install -r requirements.txt`]; app/.python-version=3.12 matches). DEPLOYMENT §3.2 vercel.json proxy URL VERIFIED CONSISTENT (app/ui/vercel.json destination `https://sansad-search-dev-api-production.up.railway.app/api/:path*` matches §3.2 char-for-char + SPA catch-all). No arch↔code gaps; Complete unaffected. **ESCALATIONS (routed, non-blocking):** (1) README.md Data Sources table (lines 28-32) stale → PROJECT MANAGER: CA sansad.in→constitutionofindia.net; LS sansad.in→Internet Archive+eparlib.sansad.in; RS rajyasabha.gov.in→sansad.in/rs+IA+rsdebate.nic.in. (2) README.md Setup section (lines 34-36) stale "run /arch" → PROJECT MANAGER: point to DEPLOYMENT §5. (3) app/pyproject.toml build-backend=`setuptools.backends.legacy:build` is INVALID (should be `setuptools.build_meta`) → CODING AGENT opportunistic; latent only (pip install . / wheel build never run; deps install via requirements.txt). (4) vercel.json live-Railway-URL confirmation + `-dev-`-in-production-hostname check → OPERATOR (Railway dashboard; not file-verifiable, no web access). Report: .agentic-reviews/arch/arch-review-phase-17-run-2.md. — QA review run-1 CLEAR 2026-06-14: no gaps. Scope = checkpoint store SQLite→PostgreSQL migration only (F01 Resumability/Deduplication + INF-R1); Phases 7–15 F01 features not re-audited (full suite is regression net). PG checkpoint tests ran for real against live DB (test-results lines 919–940, NOT skipped). Verified: INF-R1 interrupt→resume identical count/no dupes (test_checkpoint_store.py:246); §8 item-7 selective-clear re-insert invariant (test_checkpoint_store.py:274, strongest form — mirror intact yet row re-created); composite-PK cross-corpus isolation (:126); processed_documents skip/persist path (:216/:227); clear_corpus --target sqlite rejected + checkpoints PG DELETE + stage process leaves dedup mirror + date-scoped raw_documents join (test_clear_corpus.py:941–947); dry-run issues no DELETE but does SELECT COUNT. No vacuous passes. 1201 passed/0 failed/0 skipped. Non-gaps: Railway Cron Job (manual, no-test per stop condition); "no sqlite3 import" structural (arch-verified + behaviorally guaranteed by live-PG suite). Both reviews CLEAR — ready for /resume reconciliation to mark ✓ complete. Arch review run-1 CLEAR 2026-06-13: all 10 Non-Negotiables CLEAR; schema.sql processed_documents + ingestion_dedup_keys match DATA-MODELS §1.6/§1.7 exactly; store.py PostgreSQL rewrite correct (psycopg2, no sqlite3, resilience mirrors indexer.py); §8 item-7 invariant preserved in indexer.py (no has_dedup_key short-circuit); clear_corpus.py --target sqlite rejected, --target checkpoints functional (PG DELETE, stage process clears processed_documents only, stage fetch/all clears both tables, date-scoped join to raw_documents); .gitignore cleaned; main.py passes pg_dsn to CheckpointStore; Railway Cron Job open deployment task (manual, expected). Next: /verify-qa phase-17. — Build complete 2026-06-13 (first build run). Implemented DEPLOYMENT §7 task list: schema.sql checkpoint tables (§1.6/§1.7); store.py SQLite→PostgreSQL rewrite (resilience mirrors indexer.py); §8 item-7 fix — indexer no longer short-circuits on the dedup mirror (ON CONFLICT authoritative) so selective re-process re-creates rows (user-ratified); clear_corpus.py --target sqlite→checkpoints (PG DELETE); .gitignore cleaned. Tests: FakeCheckpointStore for unit suite, test_checkpoint_store.py rewritten for real PG (TEST_DATABASE_URL-gated; §8 regression + INF-R1), dedup unit tests reworked to ON CONFLICT, new test_clear_corpus.py. 1201 backend passed / 0 failed (DATABASE_URL+TEST_DATABASE_URL→local sansadsearch_test; PG-gated tests ran for real). OPEN deployment task: provision Railway Cron Job (DEPLOYMENT §3.7) — manual, dashboard-verified. Existing-deploy migration: §6.7 (no re-ingestion). |
