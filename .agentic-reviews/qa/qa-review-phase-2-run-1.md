# QA Review — Phase 2 Run 1
Date: 2026-05-28
PRD version: v1.0
Prior run: first review

## Status: GAPS FOUND

## Gaps

| # | Feature | Requirement | Gap type | Routes to | Notes |
|---|---------|-------------|----------|-----------|-------|
| 1 | F01 / main.py error logging | Error log must include source URL for every skipped document | MISSING | CODING AGENT | Test spec (Progress Log Integrity): "The error log must include the source URL for every skipped document; a skipped document with no URL reference in the log is a bug." `tests/test_ingest_main.py::TestMissingDateLogging::test_missing_date_produces_exactly_one_error_log` only asserts `len(error_records) == 1` — it does not check that the URL `http://example.com/nodatehtml` appears in any error record's `message`. Add: `assert any("http://example.com/nodatehtml" in r.getMessage() for r in error_records)`. |
| 2 | F01 / main.py completion summary | System prints completion summary with total records indexed per source, total errors, total skipped | MISSING | CODING AGENT | Feature spec acceptance: "On completion, system prints a summary: total records indexed per source, total errors, total skipped." No test in `test_ingest_main.py` verifies a completion summary is emitted at the end of a run. Add a test that runs `main()` (or whatever orchestration entry point produces the summary) against a small fixture and asserts the summary output contains per-source counts and error/skipped totals (via caplog or capsys). |
| 3 | F01 / main.py completion summary | Completion summary record count must match actual index count | MISSING | CODING AGENT | Test spec (Progress Log Integrity): "The completion summary record count must match the actual number of records retrievable from the search index after ingestion completes." No test verifies the summary count equals the indexer's accumulated `counts` per source (which in turn equal what was pushed to Meilisearch). Add a test that indexes N records via the orchestration entry point, then asserts the summary's per-source counts match `indexer.counts` and `indexer.flush()` totals. |
| 4 | F01 / indexer (OCR records) | Records flagged with `ocr_low_confidence: true` must appear in the search index; must not be silently dropped | MISSING | CODING AGENT | Test spec (OCR-Flagged Records): explicit. `tests/test_indexer.py` has no test where `ocr_low_confidence=True` is set on a record and the record is verified to be indexed (i.e. `index_record` returns True AND the row was inserted with the flag). Existing tests only cover `ocr_low_confidence=False` (the default via `_make_speech_record`). Add a test: build a speech record with `ocr_low_confidence=True`, call `index_record`, assert it returns True, and assert the PostgreSQL INSERT tuple at the `ocr_low_confidence` position equals True. |
| 5 | F01 / indexer resumability | Record count after clean run equals record count after interrupted-then-resumed run | MISSING | CODING AGENT | Test spec (Resumability): "Record count after a clean run equals record count after an interrupted-then-resumed run against the same corpus." `test_rerun_produces_zero_new_records` exercises "complete run → rerun → 0 new" (i.e. resume from a fully-checkpointed corpus). It does NOT simulate interruption-mid-corpus then resume. Add a test: process records 1–3 with checkpoint store A, simulate interrupt (close store), open store B at same path, process records 1–5 (the full corpus); assert total indexed count equals running records 1–5 in a single clean session against a fresh store. |
| 6 | F01 / main.py | `--date-override` argument must actually override the LS/RS source's end date when constructing sources | MISSING | CODING AGENT | Feature spec: "optional `--date-override` for LS/RS end date". `test_date_override` only checks that argparse stores the string — it does not verify the value is propagated to the LSSource/RSSource `date_to` / end-date parameter. Add a test that invokes the source-construction codepath with `--date-override 2020-06-01` and asserts the constructed LSSource/RSSource has that date as its end-date filter. |
| 7 | F01 / main.py | Source argument accepts `ls` and `rs` (not just `ca` and `all`) | MISSING | CODING AGENT | `TestParseArgs` covers `--source ca` and `--source all`; `--source ls` and `--source rs` are documented in the feature spec but not tested. Risk: if `choices=` is silently narrowed in argparse, this regresses without test failure. Add parametrized test covering all four valid sources, plus a negative test that `--source foo` raises SystemExit. |
| 8 | F01 / indexer | `update_index_status` test asserts only that execute was called, not what was inserted | VACUOUS PASS | CODING AGENT | `tests/test_indexer.py::TestIndexerUpdateStatus::test_update_index_status_inserts_row` only asserts `pg.cursor().execute.called`. Passes even if the SQL inserts wrong columns or wrong values. Strengthen: inspect `cursor.execute.call_args` and assert the SQL includes `INSERT INTO index_status`, and that the parameters tuple contains the expected `total_records=900` (sum of ca/ls/rs counts), `ca_count=100`, `ls_count=500`, `rs_count=300`, and the correct date ranges. |
| 9 | F01 / main.py document processing | `test_processes_html_speech_document` only checks `documents>0` and `errors==0` | WEAK | CODING AGENT | The test sets up an HTML document with a speech but never asserts `stats["indexed"] >= 1`. A bug where the segmenter returns no speeches (or the indexer silently fails) would still pass this test. Strengthen: assert `stats["indexed"] >= 1` (and ideally that `pg.cursor().execute` was called with an INSERT against `speeches`). |
| 10 | F01 / main.py progress logging | `test_indexed_record_produces_info_log` only counts INFO records, doesn't verify content | WEAK | CODING AGENT | Feature spec: "real-time progress logging (document processed, records indexed, errors, skipped)". The test asserts `len(info_records) >= 1` but doesn't verify the log content reflects meaningful progress (e.g. mentions the URL, source, or "indexed"). A log message of just `"hi"` would pass. Strengthen: assert at least one INFO message includes the URL `https://sansad.in/ls/2023/03/15/debate.html` or a recognizable phrase such as "indexed" / "processed". |
| 11 | F01 / indexer (unattributed speech) | `test_unattributed_speech_excluded_by_segmenter_not_indexer` name implies it verifies segmenter-vs-indexer boundary; assertion only tests `build_dedup_key` handles `None` | WEAK | CODING AGENT | The test docstring says the segmenter handles exclusion and asserts the indexer is defensive against `None` speaker. But the assertion `assert "unknown" in key` doesn't verify either claim — the test never invokes `Indexer.index_record` or the segmenter. Either: (a) rename the test to `test_dedup_key_handles_none_speaker_name` to match what it actually verifies, or (b) add a real test that constructs an unattributed-speech raw record, runs it through `segment_speeches`, and asserts zero records are produced (the segmenter is the one excluding it). |
| 12 | F01 / indexer reindex-from-db | `test_reindex_pushes_all_speeches_and_qa` only checks `total >= 1` | WEAK | CODING AGENT | The test loads one speech row and zero QA rows, then asserts `total >= 1` and `add_documents.assert_called()`. It does not verify: (a) all rows from both tables were processed, (b) excluded fields (`page_reference`, `ocr_low_confidence`, `has_untranslated_content`, `session_number`, `created_at`, `dedup_key`) are absent from the pushed documents (this is tested for `build_meili_document` directly, but not for `reindex_from_db`'s integration). Strengthen: load 2 speech rows + 2 QA rows, assert `total == 4`, and inspect `add_documents.call_args` to confirm the pushed documents omit the excluded fields. |
| 13 | F01 / indexer language handling integration | Phase 2 stop condition: "language handling integration (is_translated and has_untranslated_content set correctly for all four cases)" — only cases 1 and 4 tested at integration level | MISSING | CODING AGENT | `TestLanguageHandlingIntegration` covers case 1 (`is_translated=False` for English) and case 4 (`has_untranslated_content=True` for Hindi-only) at the indexer integration level. Cases 2 (Hindi+translation → `is_translated=True`) and 3 (bilingual → `is_translated=True` AND `full_text_en` non-null with both portions) are not exercised through `Indexer.index_record` and asserted at the INSERT-tuple level. Add two tests parallel to the existing two, asserting the correct field positions in the PostgreSQL INSERT tuple for cases 2 and 3. |

---

## Verified

**F01 / canonical/names.py** — strips all 6 spec-listed honorifics (Shri, Smt., Dr., Prof., Adv., Kumari) with parametrized tests; multiple-honorific handling ("Shri Dr.…"); idempotent; no-honorific unchanged; empty-string unchanged. `normalize_for_dedup` covers lowercase + underscore conversion, punctuation stripping, None/empty → "unknown", multi-space collapsing. `load_names_dict` covers valid CSV, missing file → empty dict, skipped empty variants, case-insensitive lookup. `canonicalize_name` integration: known variant → unresolved=False; abbreviated form; reversed order; unknown → stripped+unresolved=True; None/empty → (None, False); honorific stripped before lookup; **three variants produce identical canonical name** (test spec explicit); unresolved raw name preserved (not empty).

**F01 / canonical/sessions.py** — CA always returns None (even with non-None input); None/empty input → None; basic canonicalization across 8 case/punctuation variants (Budget/Monsoon/Winter Session); multi-part suffix canonicalization across 5 forms ("Part 2", "Second Part", "First Part" → "Part 1/2"); **variants of same session produce identical canonical name** (test spec explicit); LS and RS canonicalize identically.

**F01 / sources/_http.py** — fetch_with_retry: 200/404/403; 5xx retries up to max then None; 5xx succeeds on retry; **429 retries (never skipped)** including a 5-retry scenario verifying retries exceed max_retries_5xx; request error → None. RobotsChecker: allowed/disallowed; robots.txt 404 allows all; robots.txt cached per domain; network error on robots → allows all.

**F01 / sources/ca.py** — enumerates exactly 12 unique HTTPS PDFs; custom volume URLs respected; fetch_documents yields all 12 with `source=CA` and correct volume numbers; robots-disallowed volumes skipped; 404 volumes skipped without raising; content is bytes.

**F01 / sources/ls.py & rs.py** — proceeding-type inference for starred/unstarred/zero_hour and unstarred-checked-before-starred (substring trap); date extraction from URL; sitting key strips extension (HTML and PDF for same sitting share key); **HTML preferred over PDF for same sitting** (test spec explicit); pre-2014 URLs excluded; index fetch failure → empty list; **date boundary: 2014-01-01 included, 2013-12-31 excluded** (test spec explicit); robots-disallowed fetches skipped; HTML content is str, PDF content is bytes; RS source produces `source=RS`; RS also prefers HTML over PDF.

**F01 / checkpoints/store.py** — URL tracking: initially not processed, processed after mark, other URLs unaffected, count accurate, mark idempotent. Dedup key tracking: same coverage as URL tracking. **Resumability: store persists across open/close, checkpoint used to skip on resume** (test spec aligned). Context manager opens/closes; error without open raises.

**F01 / indexer.py** — Dedup key format: explicit format check for speech and QA; **same member different sequence → different keys** (test spec explicit); speaker name normalized in key; None speaker name → "unknown"; default record_type is speech. build_meili_document: **excluded fields absent** (page_reference, ocr_low_confidence, has_untranslated_content, session_number, created_at, dedup_key per DATA-MODELS.md 2.2); present fields included; None values omitted. index_record: indexes speech; skips duplicate via checkpoint store; skips record with missing date; per-source counts incremented; **same member twice in same sitting indexed separately** (test spec explicit); QA records indexed. Flush: sends batch to Meilisearch; empty batch → 0; auto-flush at MEILI_BATCH_SIZE. **Resumability at rerun: zero new records on identical second run.** reindex_from_db: pushes both tables (weak assertion — see gap #12). Language handling integration: case 1 (English not translated) and case 4 (Hindi-only still indexed) — cases 2 and 3 missing (gap #13).

**F01 / main.py** — argument parsing: `--source ca`, `--source all`, `--date-override`, `--reindex-from-db` flag and default false, missing source raises (ls/rs gaps in #7). canonicalize_record: strips honorific, sets unresolved flag, canonicalizes session name, CA session → None, None speaker unchanged. process_document: skips no-date document; processes valid HTML speech (weak — see gap #9). Missing-date logging: exactly one error logged (weak — URL not verified, gap #1). Progress logging: at least one INFO log (weak — content not verified, gap #10).

**Phase 1 regression** — all 161+ Phase 1 tests carry forward, passing in 346/347 collected items.

---

## Test execution summary

346 passed, 1 skipped (test_schema_executes_against_postgres — correctly skipped, no TEST_DATABASE_URL), 1 warning (urllib3/LibreSSL, harmless). No failures, no unexpected skips.

13 gaps total, all routing to CODING AGENT: 7 MISSING, 1 VACUOUS PASS, 4 WEAK, 1 mislabeled. No spec ambiguities — no Product Agent routes.
